import { RemovebgDirective } from './removebg.directive';

describe('RemovebgDirective', () => {
  it('should create an instance', () => {
    const directive = new RemovebgDirective();
    expect(directive).toBeTruthy();
  });
});
